set(controller_SRC
  KinovaLearning.cpp
)

set(controller_HDR
  KinovaLearning.h
)

add_controller(${PROJECT_NAME} ${controller_SRC} ${controller_HDR})
set_target_properties(${PROJECT_NAME} PROPERTIES COMPILE_FLAGS "-DKinovaLearning_EXPORTS")

